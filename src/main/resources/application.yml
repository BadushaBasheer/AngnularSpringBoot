logging:
    level:
        org:
            springframework:
                security: debug
spring:
    datasource:
        password: 1234
        url: jdbc:postgresql://localhost:5432/usermanagement
        username: postgres
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: true
